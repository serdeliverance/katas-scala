package io.github.serdeliverance

import scala.annotation.tailrec

object SumK {

  /**
    * Dado un numero k, retornar true si existen dos elementos de la lista que sumados dan k.
    * La lista está ordenada
    *
    */
  def sum(numbers: List[Int], k: Int): Boolean = ???
}
